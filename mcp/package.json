{
  "name": "forge-standard-review-mcp",
  "version": "1.0.0",
  "description": "MCP server for forge-standard-review — stdio JSON-RPC wrapping review.sh forge_review tool",
  "type": "module",
  "main": "dist/server.js",
  "bin": {
    "forge-standard-review-mcp": "dist/server.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "tsc --noEmit -p tsconfig.json && vitest run --reporter=verbose"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^5.0.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
